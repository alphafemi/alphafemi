name: Update TryHackMe Badge

on:
  schedule:
    - cron: '0 0 * * *'  # runs every day at midnight
  workflow_dispatch:       # allows manual trigger

jobs:
  update-badge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # ← updated from v3 to v4

      - name: Create assets folder
        run: mkdir -p assets

      - name: Download TryHackMe Badge
        run: |
          curl -o assets/thm_propic.png "https://tryhackme-badges.s3.amazonaws.com/${{ secrets.THM_USERNAME }}.png"

      - name: Commit and Push
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"
          git add assets/thm_propic.png
          git diff --cached --quiet || git commit -m "Updated TryHackMe Badge"
          git push
jobs:
  update-badge:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true  # ← add this
    steps:
      - uses: actions/checkout@v4
